# This is a Declarative Automation Bundle definition for databricks_concepts.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: databricks_concepts
  uuid: 0934c88c-55c0-4264-bf25-558bf54bc071

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-f90bf339-b041.cloud.databricks.com
    variables:
      catalog: ml_bronze
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-f90bf339-b041.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/viniciusbmantovam@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/viniciusbmantovam@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: ml_bronze
      schema: prod
    permissions:
      - user_name: viniciusbmantovam@gmail.com
        level: CAN_MANAGE
