# The main pipeline for databricks_concepts

resources:
  pipelines:
    databricks_concepts_etl:
      name: databricks_concepts_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/databricks_concepts_etl"

      libraries:
        - glob:
            include: ../src/databricks_concepts_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
